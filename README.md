Adweb Exercise in AngularJS and HTTP-Server

Requires : NodeJs and npm

To setup dependencies run `npm install`

To start application run `npm start` then navigate to `http://localhost:8000/` in your browser.
